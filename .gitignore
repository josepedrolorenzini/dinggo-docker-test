docker-compose.yml
.env
docker-compose.override.yml
docker-compose.prod.yml
docker-compose.test.yml
```

**Step 2 — Remove the broken lines from your `backend/.gitignore`** since they don't work:
```

./laravel-react-docker.txt
laravel-react-docker.txt